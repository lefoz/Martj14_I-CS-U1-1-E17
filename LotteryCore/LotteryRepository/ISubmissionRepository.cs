using System.Collections.Generic;
using LotteryWeb.Models;

namespace LotteryCore.LotteryRepository
{
    public interface ISubmissionRepository
    {
    //    void AddSubmission(Submission submission, List<Submission> submissionList);
    //    bool AddSerialToExistingSubmisson(List<Submission> submissionList,string email, string serial);
    }
}